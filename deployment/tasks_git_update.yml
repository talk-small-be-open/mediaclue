- name: git repo Mediaclue base code
  become_user: "{{remoteUser}}"
  git:
    repo: git@github.com:talk-small-be-open/mediaclue.git
    dest: "{{mediaclue.installationPath}}"
    update: yes
    force: yes
